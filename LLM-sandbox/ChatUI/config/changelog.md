---
summary: Full history of changes to ChatUI — the old single-file chatui.py (through 2026-07-04) and the chatui/ package rebuild (from 2026-07-04 on).
---

# Changelog

Two sources of change, both recorded here:
- **claude** — direct edits made by Claude Code in a session
- **pipeline** — CHANGE:/FIX: directives applied via `/update`+`/apply` or `apply_update.py` (retired as of the 2026-07-04 rebuild — no longer a source of new entries)

Full session context for select sessions worth preserving beyond this log's one-line-per-change format lives in `LLM-sandbox/conversations/`.

---

## 2026-06-24

| Source | What | Why | Commit(s) |
|--------|------|-----|-----------|
| claude | Initial chatui.py rewrite — Textual TUI, /command interface, session auto-save | Replace the script-style obsidian_brain.py with a full terminal app | `ed43ff2` |
| claude | Add /browse, streaming responses | UX — navigate vault files as context; responses feel instant | `2ae50cf` |
| claude | Add config/ system, model separation, /update groundwork | Separate tuneable params from code; foundation for self-update | `6434e08` |
| claude | /update pipeline v1 (unified diff approach) | Let the app modify itself from config changes | `02956ea`→`f5a6725` |
| claude | **Rewrite /update pipeline** — git diff + surgical AST edits + ast.parse validation | v1 used line-numbered unified diffs which OOM'd on 14b model and broke on any mis-numbering; new approach sends only the relevant function block (~30 lines) to a 7b model | `0edbf91` |
| claude | /version command generated end-to-end via /update + /apply | First live test of new pipeline | `8bd14b4` |
| claude | Update README; clean up models.md (remove qwen2.5-coder:14b) | README was stale; 14b model caused the OOM crash | `875d844` |
| claude | /web state in header; fuzzy command matching; /status command; fix /version help line | UX issues spotted from reviewing 40 session logs | `0001e59` |
| pipeline | All `/command` output appended to session log files | Commands left no trace in conversation history | `1b8502c` |
| claude | Include `conversations/` in ChromaDB ingestion | Session logs contain useful context for retrieval | `23db904` |

## 2026-06-25

| Source | What | Why | Commit(s) |
|--------|------|-----|-----------|
| claude | UI overhaul: busy-bar loading indicator, wider margins, /stats command | Users couldn't tell if app was processing; layout ran wall-to-wall | `7fe0492` |
| claude | Smarter chunking (MarkdownHeaderTextSplitter); note deduplication (similarity ≥ 0.85 → append); --vault CLI arg; fix /organize + /apply not saving to log | Chunking by character boundary split mid-sentence; duplicate notes accumulated | `fca3189` |
| claude | Granular status labels ("Searching vault…" etc.); vault answers show source file attribution | Generic "📓 Source: vault notes" gave no useful signal | `35be41e` |
| claude | /edit command (guided file edits via LLM diff); /daily command (session summary → daily.md) | Pipeline for improving vault notes from inside the app | `9243236` |
| claude | Watchdog auto-reingest on vault file change; /export training data (jsonl/alpaca/csv) | Avoid manual /ingest after every edit; enable model fine-tuning | `a200842` |

## 2026-06-26

| Source | What | Why | Commit(s) |
|--------|------|-----|-----------|
| claude | LLM-efficient session format: YAML frontmatter, HTML comment audit trail, INDEX.md | Scanning conversations/ was token-heavy; needed a compact one-line-per-session index | `2e663aa` |
| pipeline | Add /readme command | Regenerate README from live source so it stays accurate as commands evolve | `39d9fdb` |
| pipeline | Add /harvest command | Promote conversation topics into vault stubs automatically | `39d9fdb` |
| pipeline | FIX _cmd_harvest — strip interrogative starters; add fuzzy vault matching | Harvest creating topics like "What is fine-tuning" instead of "fine-tuning" | `61a22b1` |
| claude | Fix two micro-bugs in harvest (direct): leading-space broke ^ anchor; comma-split fragmented compound topics | Pipeline applied the fix but model missed these edge cases | `9af06a4` |
| pipeline | FIX _cmd_harvest — 2-word concept name extraction; strip articles + trailing connectors | Slugs like "fine-tuning-in-the-context-of-large-lang.md" were useless filenames | `33d9d30` `5c0f604` |
| claude | Add self-review pass to apply_update.py; improve decorator/indent handling | First-pass fixes had ~20% error rate (dropped decorators, wrong indentation) | `940ae25` `e54064e` |
| pipeline | FIX _directive_model_guided — broaden context regex to any `_func_name` | FIX directives for non-`_cmd_*` functions (e.g. _finalize_session) had no context | `7c9fd72` |
| pipeline | FIX _generate_new_method — add self-review pass to TUI pipeline | Self-review existed in apply_update.py but not in the TUI /update+/apply flow | `7c9fd72` |
| pipeline | FIX _cmd_harvest — add `slug in stem` substring fallback | "math" (4 chars, below fuzzy threshold) wasn't matching Mathematics.md | `7c9fd72` |
| pipeline | FIX ingest_vault — exclude conversations/INDEX.md from ChromaDB | INDEX is navigation metadata, not knowledge; was polluting retrieval results | `7c9fd72` |
| pipeline | FIX _finalize_session — LLM keyword extraction for topics field | Regex strip still produced low-quality topic strings | `1b6d28c` |
| claude | Fix _finalize_session [:4] slice bug in pipeline-generated code | Model wrote `"; ".join(...)[:4]` (string slice) instead of `items[:4]` (list slice) | `1b6d28c` |
| claude | apply_update.py: race guard (skip if block already replaced); broaden fn-name regex to snake_case; flush=True on all prints | Two parallel runs could both apply same FIX; non-`_` functions had no context; output was invisible during long LLM calls | `7c9fd72` `1b6d28c` |
| claude | Track conversations/ in git; add .chatui_sync to .gitignore | Session logs and transcript should be versioned; sync hash is machine-local state | `ab4eec3` |
| claude | **Fix ChromaDB persistence** — swap to `chromadb.PersistentClient` + `collection_name="vault"` | ChromaDB v1.x dropped `persist_directory=` API; local_db/ was never written to disk, vault lost on every restart | `bac2cce` |
| claude | Rename `Machine Learning.md` → `machine-learning.md` | Spaces in filenames break shell tools and wikilinks | `bac2cce` |
| claude | Delete obsidian_brain.py, ChatUI.md, test.txt, "first file!.md" | Dead code and junk files cluttering vault root | `bac2cce` |
| claude | Clean settings.md — move applied directives to changelog.md; fix stale Architecture section | settings.md had 16 applied directives buried in it; Architecture section described old patch-based pipeline | `bac2cce` |
| claude | Add /distill command — extract Q&A pairs from a session file, bootstrap `_article-guide.md` if missing, generate structured articles with taxonomy-aware YAML tags and [[wikilinks]], append to existing vault files | Core workflow: conversation logs → rich vault articles that expand over time | `ef3094c` |
| claude | /distill first run — 24 vault articles generated from session `2026-06-24_23-52-49`; fix prompt bugs: strip raw Q&A artifact lines from source, tighten wikilink constraint to vault-only stems, add frontmatter fallback | First live run exposed prompt contamination and broken wikilinks | `42aca01` |
| claude | Fix /distill frontmatter — strip model-generated `---` blocks, generate title + tags programmatically from slug + keyword map; add `_distill_tags()` helper; retag all 22 existing articles | llama3.2:3b consistently produced unclosed frontmatter; hyphen-vs-space mismatch in keyword map caused most articles to fall through to `[general]` | `95a7340` |
| pipeline | Add FIX directives to settings.md — wikilink post-filter in /distill; RAG grounding for article generation | Articles linked to non-existent vault pages; content drifted from source material | `61f0b43` |
| claude | Vault pre-seeding: 6 reference files (`_ref-lora.md`, `_ref-rlhf.md`, `_ref-fine-tuning.md`, `_ref-rag.md`, `language-models.md`, `machine-learning.md`); quality fixes to 7 articles (corrected LoRA mechanism, RLHF pipeline, frontmatter); /distill wikilink post-filter — strip `[[X]]` where X doesn't match any vault stem | Articles had factual errors (e.g. LoRA description was backwards); wikilinks pointed to non-existent pages | `ba0202d` |

## 2026-06-27

| Source | What | Why | Commit(s) |
|--------|------|-----|-----------|
| claude | `_cmd_ingest` updated for new `ingest_vault(force) -> tuple[Chroma, dict]` return signature; displays granular stats (new/updated/removed/unchanged counts) | Signature change from prior session caused a crash on /ingest | `4a40d3f` |
| claude | `vault-structure-plan.md` created — full Dewey hierarchy, current file placement, tag→folder mapping table, deviation rationale, `_ref-*` special case | Living document for vault organisation; ingestible so the model can answer structure questions | `4a40d3f` |
| claude | Pass 4 added to `_run_organize()` — file placement using Dewey-based folder structure; `_needs_placement()`, `_classify_for_placement()` helpers; `_TAG_TO_FOLDER`, `_DEWEY_FOLDERS`, `_PLACEMENT_SKIP_TAGS` constants | Vault files were accumulating at root with no folder structure | `780530f` |
| claude | `_run_organize()` scan fixed — changed from `glob.glob(VAULT_PATH/*.md)` to `_discover_vault_files()` so subdirectory files are included | Pass 1 was skipping all files that had already been moved into subfolders | `780530f` |
| claude | Merge 15 per-minute session files into 2 daily files (`2026-06-24.md`, `2026-06-27.md`); ChatUI `on_mount` updated to append to a single `YYYY-MM-DD.md` per day | Session files were split by the minute; hard to navigate and redundant | `bb13670` |
| claude | `_finalize_session` recognises `YYYY-MM-DD.md` pattern; `_rebuild_conversation_index` counts sessions per daily file; `_cmd_daily` reads single daily file | Supporting changes for daily-file format | `bb13670` |
| claude | Update claude_transcript through Session 9; delete stale duplicates (`claude_transcript 1.md`, `INDEX 1.md`) | Transcript was out of date; duplicate files from Obsidian sync | `fdf9fd4` |

## 2026-06-28

| Source | What | Why | Commit(s) |
|--------|------|-----|-----------|
| claude | Project assessment — identified structural duplication (two chatui.py copies, two vault locations), broken `vault_path`, two pending FIX directives | Full audit of project state with three-phase remediation plan | _(no commit)_ |
| claude | Move canonical code to `ChatUI/`; delete stale root copies of `chatui.py`, `apply_update.py`, `_article-guide.md`, `docker-compose.yml` | Root copies were 262 lines behind ChatUI/ and diverging | _(this commit)_ |
| claude | Rename all 37 `Knowledge/*.md` files from kebab-case to Title Case with Spaces; `_ref-*` → `_Ref Title Case` | Consistent naming convention matching Obsidian display style | _(this commit)_ |
| claude | Update `chatui.py` line 1653: `"taxonomy.md"` → `"Taxonomy.md"` | Hardcoded filename reference broke after vault rename | _(this commit)_ |
| claude | Delete `ChatUI/config/` duplicates: `Changelog 1.md`, `Commands 1.md`, `Models 1.md`, `Settings 1.md`, `Improvement Notes.md` | Obsidian sync created versioned duplicates; `ideas.md` was already the superset | _(this commit)_ |
| claude | Merge all 17 divergent root `.md` files into `Knowledge/` counterparts (append unique content), then delete root copies | Root was the old flat vault; `Knowledge/` is now the single canonical vault location | `d04811c` |
| claude | Add `llama3.1:8b` as default `chat_model`; both models in auto-pull registry; `/model [name]` runtime switcher — swaps `llm` globally, updates header subtitle via `self._chat_model` | 8b gives ~3× capacity over 3b for same disk footprint as coding model | `39a1c21` |
| claude | Retrieval config: `top_k 3→5`, `chunk_size 500→800`, `chunk_overlap 50→100` | Larger Knowledge/ articles split mid-section at 500 chars; 3 chunks allowed crowding-out | `39a1c21` |
| claude | Fix web fallback — remove `uncertain` phrase gate; web now fires whenever in model-knowledge path and web is on | `llama3.2:3b` almost never emits `"i'm not certain"` so fallback never triggered | `39a1c21` |
| claude | Raise `similarity_threshold` 0.5→0.65; lower `history_window` 4→2; update `build_vault_prompt` to allow training-knowledge supplement | 8b model was false-positive matching AI-adjacent questions at 0.55–0.63 (vault path fired, bypassing web); history_window=4 caused RLHF answer to contaminate next EU-regulation query; vault prompt said "say so clearly" when context insufficient, causing model refusal | `a877541` |
| claude | Fix `build_grounded_prompt`: answer-first from training knowledge; include vault notes only if directly relevant | Grounded path was blending off-topic vault chunks (EU regulation answer got Constitutional AI tail from `Agential Patterns.md` at score 0.60) | `4344119` |
| claude | Create `CLAUDE.md` — repo layout, constraints, self-update docs, end-of-session checklist, retrieval table, common pitfalls | No project context file existed; each session re-derived structure from git log | `3aafc44` |
| claude | Rewrite `README.md` — update default model to llama3.1:8b, add `--vault` flag, full command list, retrieval pipeline, stack table | README was stale: wrong default model, missing /model and /distill, no retrieval docs | `3aafc44` |
| claude | Add `Stop` hook to `~/.claude/settings.json` — warns at session end if transcript/changelog/README not updated | Prevent checklist items from being forgotten across sessions | `3aafc44` |
| claude | Fix 9 command bugs found during audit: `/help` markup (`[name]`/`[[links]]` stripped by Rich); `_apply_queue` swallowing `/commands`; `/readme` destructive overwrite; `/model` silent bad-name accept; `/status`+`/stats` merged; `/export` duplicate line; `/distill` lists sessions; `/harvest` clearer error; `/daily` acknowledges existing file | Full command audit identified rendering bugs, silent failures, and UX gaps | `21c9e4a` |
| claude | Apply pending `/distill` directive: RAG grounding — query vault per Q&A pair before building `art_prompt`, inject top-2 chunks (score ≥ 0.45) as "REFERENCE MATERIAL" block; directive 1 (wikilink filter) was already implemented in Session 7 | Directive had been in `settings.md` since `61f0b43`; bypassed git-diff pipeline since commit predated any usable sync hash | `4ee93fd` |
| claude | Fix two bugs blocking vault folder structure: (1) `_cmd_distill` `os.listdir` → `_discover_vault_files()` for post-move wikilink resolution; (2) `_is_ref()` helper to detect `_Ref Title Case` filenames (not just `_ref-`) | Both would have silently broken after files moved to subfolders | `00cf05d` |
| claude | Run `/organize`: wikilinks added to `Natural Sciences.md`, `Social Science Subfields.md`; all 42 files placed into Dewey subfolders; 4 `_Ref` files to `600-applied-sciences/_ref/`; Cupcakes corrected to `misc/`; Pass 3 bug fixed (INDEX.md excluded from condense candidates); ChromaDB rebuilt at 309 chunks | vault-structure-plan.md implemented | `46f87ad` |

## 2026-07-02

| Source | What | Why | Commit(s) |
|--------|------|-----|-----------|
| claude | Fix `/distill`: articles now routed through `_classify_for_placement` into the correct Dewey folder instead of always landing in `VAULT_PATH` root; filename and frontmatter `title` now derived from the topic slug in Title Case With Spaces instead of `slug + ".md"` and the raw question text | `/distill` produced `classification-standards--library-systems--subject.md` in the vault root with the raw question as its title — violated both the vault's filing convention and `_article-guide.md` rule 5 | _(this commit)_ |
| claude | Widen tag-scoped retrieval pass from `k=TOP_K` to `k=TOP_K * 2` (chatui.py:2870) | Once a query narrows to one tag, a 12+ file topic cluster (e.g. taxonomy) could still only surface ~5 files at the old `k`; direct DB query confirmed relevant files (Faceted Classification, Polyhierarchy, Taxonomy Mappings) were losing the retrieval lottery to less relevant chunks purely on count | _(this commit)_ |
| claude | Add See Also specificity instruction to `/distill` article prompt — prefer notes on the same specific subtopic over notes sharing only a broad field | Distilled articles were linking weakly-related notes over more specific existing ones | _(this commit)_ |
| claude | Delete misfiled `classification-standards--library-systems--subject.md` | Content was a near-total duplicate of `Library of Congress Classification System.md` and `Folksonomy Differences.md`; relocating it would have kept the duplication, not fixed it | _(this commit)_ |
| claude | Rewrite `000-information/INDEX.md` — reflect actual `000/300/500/600/misc` folder split and current Title Case filenames | Previous INDEX.md still listed pre-reorg lowercase-dash filenames from before the Session 16 vault restructure; gets embedded and retrieved, so it was feeding stale context into answers | _(this commit)_ |
| claude | Log consolidate-all-taxonomy-into-one-file idea in `config/ideas.md` (not adopted) | User proposed it; recommended against due to retrieval/backlink tradeoffs, kept for later reconsideration | _(this commit)_ |
| claude | Fix stale venv path in `CLAUDE.md` run instructions (`../.venv` → `.chat_venv`) | Actual venv is `ChatUI/.chat_venv`; the documented path no longer existed | _(this commit)_ |
| claude | Harden `web_search()` call sites with `asyncio.wait_for(timeout=20)` plus explicit `CancelledError`/`Exception` handling | Class-question auto-web-supplement hung indefinitely on a stalled DDG connection; fix did not fully resolve the hang (see testing notes in `ideas.md`) but improves failure visibility | _(this commit)_ |
| claude | Delete 3 duplicate/mis-named `/distill` articles (`Local Regularization Technique.md`, `Classification Systems.md`, `Library Classification.md`) and consolidate 2 near-duplicate RDF/OWL articles into one `Semantic Web And Ontologies.md`; rename `Graph Vs Taxonomy.md` → `Knowledge Graph vs Taxonomy.md` | `/distill` has no cross-file dedup (unlike `/savefile`'s 0.85-similarity check) and is not idempotent across repeated runs on the same session file — see `ideas.md` testing notes | _(this commit)_ |
| claude | Add `Knowledge Graph vs Taxonomy.md` and `Semantic Web And Ontologies.md` to `000-information/INDEX.md` | New genuinely-new-topic articles from overnight testing session | _(this commit)_ |

## 2026-07-03

| Source | What | Why | Commit(s) |
|--------|------|-----|-----------|
| claude | Add semantic-similarity dedup to `/distill` (same pattern as `save_to_vault`'s dedup for `/savefile`), threshold tuned to 0.7 from a direct measurement rather than copying `/savefile`'s 0.85 (which never fired for this query shape — true duplicates measured ~0.69) | `/distill` had no dedup at all, unlike `/savefile` | `832c65c` |
| claude | Add `<!-- distilled -->` marker insertion so `/distill` is idempotent across repeated runs on a growing session file | Re-running `/distill` on the same file reprocessed all Q&A pairs from scratch every time, regenerating duplicate articles under new hallucinated names each run | `832c65c` |
| claude | Migrate `web_search()` from `duckduckgo_search` (deprecated upstream) to `ddgs` | Package renamed upstream; `ddgs` is the maintained successor with a compatible `DDGS` API | `832c65c` |
| claude | Add `_validate_wikilinks()` — normalises `[[wikilinks]]` against real vault stems (hyphens/spaces/case) and rewrites stale ones; wired into `/organize` as Pass 5 | No existing mechanism validated existing wikilinks, only added new ones | `832c65c` |
| claude | Ran `_validate_wikilinks()` directly against the vault: fixed 58 stale wikilinks across 24 files | Pre-rename kebab-case links (`[[fine-tuning-methods]]`, `[[taxonomy]]`, etc.) were broken since the Session 16 Title Case migration — far more widespread than the ~6-file estimate from the prior session's finding | `832c65c` |
| claude | Cleaned up 9 duplicate `/distill` articles generated while validating the dedup fix; trimmed two redundant "Additional notes" appendices from `Knowledge Graph vs Taxonomy.md` | Live validation run confirmed the threshold-tuning finding before the code fix was live | `832c65c` |
| claude | Research session: 8 Q&A pairs on browsable databases (Wikipedia category graph, Google crawl/index/rank, faceted search, B-trees, inverted indexes); distilled into 8 vault articles in `000-information/` and `600-applied-sciences/`; exported 39 Q&A pairs to `training_data.jsonl` | Grow vault coverage on information retrieval fundamentals | `c8d89e6` |
| claude | Fix `/distill` article quality: increase `guide_ctx` read cap `[:700]`→`[:2000]`; inject `guide_ctx` into `art_prompt` as leading "ARTICLE WRITING GUIDE" block; replace vague FORMAT line | `guide_ctx` was loaded but never used — the article guide had zero effect on generated output | _(this commit)_ |
| claude | Add four anti-patterns to `_article-guide.md`: no Q&A format output, no "I couldn't find" responses, no verbatim source reproduction, no invented facts | These are the failure modes visible in broken articles like `Taxonomy.md` | _(this commit)_ |
| claude | Add shared guided-review framework: `Proposal` dataclass, `ChatApp._review()` controller, `ProposalEditScreen` modal (Textual `TextArea`); wire into `/organize` Passes 1/2/4/5 (tags, wikilinks, placement, wikilink-fix) and `/distill`'s article-write step | Implements the `ideas.md` General-section proposal to replace hard-coded automated passes with a guided accept/skip/edit/all/none review; `/distill` previously wrote articles with no review step at all, a root cause of inconsistent article quality | _(this commit)_ |
| claude | Add `config/organize_feedback.md` log + `_log_organize_feedback()`/`_load_organize_feedback()`; inject past-correction context into the tag, wikilink, and placement suggestion prompts | Closes the loop the idea asked for: manual overrides during review should improve future suggestions, not just be silently accepted | _(this commit)_ |
| claude | Phase 2: split-pane review UI — `compose()` restructured into `#main-body` (`Horizontal`) with a docked `#review-panel` (`ListView` + detail `Static`, hidden by default) on the left and the existing chat in `#chat-pane` on the right; `_review()` now populates the panel with the whole batch up front and updates per-row status icons (pending/accepted/skipped/edited) live; clicking the active row is equivalent to typing `edit` | Delivers the "chat on the right, file/organization menu on the left" layout from the original idea; a pure rendering layer on top of the Phase 1 `Proposal` model, no interaction-logic changes needed | _(this commit)_ |
| claude | UI refinement pass on Phase 2: fix `MissingStyle` crash (any `proposal.kind`/`[[wikilink]]` text read as an invalid Rich style tag by `Static.update`'s markup parser — every review-panel row was affected); fix phantom horizontal scrollbar on `#log` once the review panel narrows it (`RichLog`'s 78-col `min_width` floor, plus `overflow-x: hidden` as a second line of defense); fix `ProposalEditScreen`'s help line being clipped off-screen (fixed-height `TextArea` overran the modal box once header/help margins were counted — switched to `height: 1fr`); fix silent corruption of `[[wikilink]]` text in the existing chat-log display (same markup-parsing issue, present before Phase 1/2 but only now caught) | Found by actually rendering the UI (Textual `export_screenshot()` → SVG → PNG) instead of only driving it with assertion-based pilot tests, which never forced a real paint and so never triggered the crash | _(this commit)_ |
| claude | Tidied `config/ideas.md` — removed ~130 lines of resolved/stale content (superseded by `changelog.md`/`claude_transcript.md`), pulled the still-genuinely-open threads forward (verified each against the current code rather than assumed) | User asked for a cleanup pass; the file had accumulated a full session-testing history alongside the actual backlog, most of it already resolved | _(this commit)_ |
| claude | Fix `/organize` looking frozen during LLM generation — `_cmd_organize` never called `_set_busy(True, ...)`, so the busy-bar (used everywhere else in the app) stayed invisible for the entire run; add `ChatApp._await_with_progress()` helper that ticks an elapsed-time counter onto the busy-bar during any single long `await`, wired into Pass 1's batched tag call, Pass 2/4's per-item loops (with "(i/N)" + filename), Pass 3's per-session summary, and `/distill`'s article-generation call | User watched a live `/organize` run sit at "Scanning for wikilink opportunities…" for minutes with zero feedback and flagged it as the same complaint already logged in `ideas.md`'s UX section | _(this commit)_ |
| claude | Add upfront checkbox file picker to `/organize` — `ChatApp._pick_files_to_organize()` shows a checkbox list in the review panel before Pass 1, pre-checking files missing frontmatter or unplaced (`[unorganized]` tag), `all`/`none`/`unorganized` bulk commands plus per-row click toggling; `selected_stems` gates Pass 1/2/4's iteration while `notes`/Pass 2's wikilink-target pool stays built from the full vault (avoids narrowing valid link targets down to just the selection) | Closes the other half of the `ideas.md` guided-`/organize` idea — Phase 1+2 shipped the review step but `/organize` still unconditionally processed every vault file with no way to scope a run | _(this commit)_ |
| claude | Full front-end refactor, four milestones — **A (theme)**: `ChatApp.ansi_color = True` + `background: transparent` on `Screen`/`Header`/`Footer`/`RichLog`/`#review-list`/`#file-tree` so the terminal's own background/colorscheme shows through (verified live: zero background escape codes emitted); `scrollbar-size: 0 0` hides scrollbars; consolidated repeated hex into `$border`/`$accent`/`$dim`/`$text`. **B (resize)**: new `Splitter` widget (`MouseDown`/`MouseMove`/`MouseUp`) between `#review-panel` and `#chat-pane`, drag to resize within 24–100 columns. **C (tree picker)**: new `FileTree` (subclasses `Tree`) replaces the flat-list file picker with real expandable folder nodes; `Ctrl+A`/`Ctrl+R`/`Ctrl+U`/`Ctrl+Enter` keybindings (shown in the footer) replace the typed `all`/`none`/`unorganized` Input commands. **D (interaction model)**: new `ReviewListView` replaces `_review()`'s per-item typed `accept/skip/edit/all/none` loop — every `Proposal` defaults to accepted and shows at once; Space toggles skip/include on the highlighted item, Enter/click opens the edit modal, Ctrl+Enter applies everything checked in one pass. `Proposal`'s data model and the override-reason feedback log are unchanged; only the interaction shell around them was replaced. New semantic: escaping an edit modal now means "no change" rather than a forced skip. | User reviewed their own manual test session in the conversation log — a 2-hour `/organize` run that was mostly the user away at unanswered per-item prompts (one placement prompt sat 63 minutes) — and asked for a full refactor rather than incremental UX patches | _(this commit)_ |

## 2026-07-04

| Source | What | Why | Commit(s) |
|--------|------|-----|-----------|
| claude | Stream Pass 1/2's LLM calls token-by-token instead of a single blocking `invoke()` — new `ChatApp._stream_lines()` switches to `coding_llm.astream()`, updating `#review-detail` live with raw tokens (the same "watch it type" effect as chat answers) while a growing `ListView` in the review panel shows each recognized `stem: tags` / `"phrase" -> target` line the instant it completes (`✓ Taxonomy.md: taxonomy, classification`) | User watched a live `/organize` run sit on "Checking [file] for wikilinks… (5/5) (131s)" with only the elapsed-time ticker as a signal, and asked for streaming "just like how prompts show each token" — the busy-bar ticker (added in an earlier session) fixed the "is it frozen" question but not the "what is it actually doing" one | _(this commit)_ |
| claude | Checkpoint real vault/session content, then wipe `chatui.py`/`apply_update.py` entirely in a separate commit | User redefined ChatUI's purpose around four verbs (Ask/Draft/Classify/Review — see `project_chatui_redefinition` memory) after flagging that a full session had scope-crept into polishing `/organize`'s batch-review UI without touching the app's actual purpose; asked to rebuild from scratch, using old commits as reference for reusable utilities rather than the deleted file itself | `d6d0b2f`, `1fb5c9b` |
| claude | Add `config/style_guide.md` — naming conventions (functions/classes) distilled from PEP8 §Naming, Clean Code Ch.2–3, Effective Python Items 30–39 | User provided coding-guideline PDFs and asked specifically for function/class naming standards, having noticed poorly-named functions in past projects | `f01c352` |
| claude | Add async/concurrency (Clean Code in Python Ch.7) and classes/data-modeling (Clean Code Ch.6) sections to `style_guide.md`; replace vague "Open" note with a concrete per-source backlog of undistilled chapters | Textual/asyncio correctness is load-bearing for this app (the `NoActiveWorker` bug class), and objects-vs-data-structures discipline directly informs the rebuild's class design | `eedd22d` |
| claude | Add error-handling section to `style_guide.md` (Clean Code Ch.7 + Ch.8 opening — raise don't return sentinels, wrap third-party exceptions at the boundary, don't return/pass `None`) | Directly needed while designing the rebuild's exception policy (`chatui/errors.py`) | `004ff96` |
| claude | Design and scaffold the `chatui/` package skeleton — 15 files (`errors.py`, `models.py`, `config.py`, `vault.py`, `retrieval.py`, `llm.py`, `web.py`, `feedback.py`, `ask.py`, `draft.py`, `classify.py`, `review.py`, `app.py`, `ui/streaming.py`, `ui/picker.py`, `__main__.py`), every function with a real signature and an unimplemented (`NotImplementedError`) body, replacing the old module-level globals (`llm`/`coding_llm`/`VAULT_PATH`) with objects (`Vault`, `ModelClient`, `Settings`) owned by `ChatApp`. Confirmed with the user: drop the `/update`/`/apply` self-update pipeline entirely (doesn't map to any of the four verbs); package+objects over single-file+globals (testability). Entrypoint is `python -m chatui` (a sibling `chatui.py` script would collide with the `chatui/` package name). An Explore pass over the deleted `chatui.py` (via `git show 1fb5c9b~1:...`) catalogued reusable logic (frontmatter parsing, wikilink scanning, ChromaDB chunking/ingest, the retrieval-path decision tree, the ddgs web wrapper, the streaming pattern, the suggest/override/feedback loop) that the skeleton's functions are designed to receive, not reinvent | User asked to plan a skeleton of classes/functions/methods for the rebuild, then asked to keep going autonomously while away for the day, logging any open questions for their return (6 logged in the plan file at `~/.claude/plans/jaunty-baking-hearth.md`) | `004ff96` |

## 2026-07-06

| Source | What | Why | Commit(s) |
|--------|------|-----|-----------|
| claude | Add `config/ui_style_guide.md` — distills Refactoring UI, Laws of UX, Don't Make Me Think, and The Design of Everyday Things, each section tied back to a real decision in this project rather than generic notes; explicit backlog entries for anything skipped | User asked to find and distill well-rated UI/UX books ahead of building the real interface | _(this commit)_ |
| claude | Build a real, interactive throwaway prototype (`ChatUI/_demo_ui.py`, explicitly NOT part of the `chatui` package): wikilink walkthrough (per-candidate highlight-in-preview + apply/skip, not a batch popup), auto-scroll-to-highlight in the preview pane, `chatui/ui/theme.py` (named color/spacing scale replacing every ad-hoc hex literal), faded-accent status text, `RichLog` word-wrap fix (`min_width=78` default was forcing overflow off-screen), `FilePicker` ellipsis truncation (Tree has no wrap support at all — a real framework limitation, disclosed not hidden), `CHAT_WIDTH` 50→70, fuzzy file search (`FilePicker.filter_files()` via `textual.fuzzy.Matcher`, the same matcher Textual's own command palette uses) replacing the static "vault/f2" header, command autocomplete (`Input(suggester=SuggestFromList(...))`, Tab wired via a `priority=True` binding since `Screen`'s own plain "tab" binding otherwise wins), `/explorer`/`/palette` command aliases, `Footer`/`Header`-icon removal, unified preview-is-draft-target state | User drove a full UI research-then-prototype pass: distill 4 books, implement + review against the notes, then a sequence of concrete refinements (scroll-to-highlight, color/spacing system, faded orange + wrap, resizable-panes question resolved by widening `CHAT_WIDTH` instead, fuzzy search, autocomplete, command aliases, chrome cleanup) | _(this commit)_ |
| claude | Implement the real `chatui/app.py` (previously all `NotImplementedError` stubs), wiring the prototype's validated layout/interactions to the real `ask`/`draft`/`classify`/`review` verb modules; add `FilePicker.reload()`; fix `__main__.py`'s `Retriever()` call never passing `chunk_size`/`chunk_overlap` from settings (silently used library defaults instead) | User said the prototype was "polished enough to take to the real program" | _(this commit)_ |
| claude | Fix a real bug found only by real-service integration testing: `ChatApp._resolve_review()` called `review.mark_reviewed()` (returns an updated copy, per the dataclass-`replace()` pattern) but discarded the return value, so `last_reviewed` never actually updated after finishing a review walkthrough | A fake-model smoke test passed regardless (it doesn't check timestamps deeply); only surfaced running the real flow end-to-end against actual Ollama models and a throwaway ChromaDB instance | _(this commit)_ |
| claude | Update `CLAUDE.md` (repo layout, run command, module-globals→objects note, drop self-update section, fix stale `ChatUI/conversations` path to `Knowledge/conversations`) and `README.md` (mark rebuild status, new run command, four-verb command surface, drop self-update section) to match the rebuild | Both were still describing the deleted single-file app; CLAUDE.md's `ChatUI/conversations/` path was already stale independent of the rebuild (moved to `Knowledge/conversations/` in `eb9a0f5`) | `004ff96` |
| claude | Fix a syntax error in `app.py` `_start_review` — a leftover in-progress edit left an unclosed `files_due_for_review(...)` call plus a commented-out `_REVIEW_STALENESS` constant, so the module wouldn't import | Surfaced by `py_compile` at the start of a style review; the whole app was unimportable | `_(this commit)_` |
| claude | Add a `review_staleness_days` setting (`config.py` `_DEFAULTS`/`Settings`, `settings.md` frontmatter + table, wired into `app.py` `_start_review`) replacing a hardcoded `timedelta(days=30)` | The `/review`-with-no-file-selected due interval was a lone inline literal; it's the one review knob a user might actually tune (weekly vs monthly), so it belongs with the other `settings.md` params | `_(this commit)_` |
| claude | Comment/docstring style pass across the package (`app.py`, `ui/picker.py`, `ui/streaming.py`, `ui/theme.py`, `tests/test_ask.py`) to the domain-module house style: one-line imperative docstrings, design *why*s split out of docstrings into `#` comments, and every author/book/design-doc provenance citation removed (`per Norman`, `(CLIG)`, `the artifact`, `ui_style_guide.md`, `Refactoring UI`, `Jakob's Law`) | `app.py` and the new UI files had drifted verbose and citation-heavy relative to `ask.py`/`vault.py`; user asked for a pristine pass and for the standard to be enforced from now on | `_(this commit)_` |
| claude | Sharpen `config/style_guide.md`: comments/docstrings must stand on their own (no external provenance, only a short in-repo spec pointer allowed); marker-class / self-describing-DTO docstring carve-out; module docstrings one line, no provenance | Encode the exact rules the style pass applied so code and guide can't drift apart | `_(this commit)_` |
| claude | Add an enforced "Code style — apply while writing" section to `CLAUDE.md` (load-bearing comment/docstring rules in-context + pointer to `style_guide.md`); fix two stale "skeleton-only / nothing functional" lines now that the package is implemented | User wanted the rules in Claude's per-session context so no formatting pass is needed after future changes | `_(this commit)_` |

## 2026-07-16

| Source | What | Why | Commit(s) |
|--------|------|-----|-----------|
| claude | Add `chatui/debug_log.py` — a single gitignored `chatui_debug.log` file that every module writes to via `logging.getLogger(__name__)`, covering LLM prompts/responses, retrieval path/score decisions, ingest stats, vault saves, dispatched commands, and every caught `ChatUIError` that was previously shown in the UI and then discarded | User asked for proper debug prints to a single log file so future bugs can be diagnosed by reading the log instead of live-testing the TUI | `f4954f7` |
| claude | Split `ClassificationSuggestion`'s wikilink fields out into `_suggest_links` returning `(kept, already_linked)`, so `/wikilinks` can distinguish "model proposed nothing" from "model proposed something, but it's already linked" — `app.py` now shows `wikilinks: already linked to X` instead of the ambiguous `no wikilinks suggested` | Live-debugged a user report ("ran /wikilinks, got no wikilinks suggested") using the new debug log; confirmed the model *had* proposed `Machine Learning` but it was correctly filtered as already-linked, not a retrieval bug | `f4954f7` |
| claude | Fully split `classify.suggest_classification` (tags+folder only) from a new `classify.suggest_wikilinks` (links only, its own `on_status` callback); `/wikilinks` no longer runs tags/folder suggestion at all, so it only ever reports `searching for wikilinks in X.md…` | User: "/wikilinks... should only say searching for wikilinks in *.md because that's what /wikilinks does" | `f4954f7` |
| claude | Applying a wikilink now wraps the actual phrase inline as `[[Title]]` (`classify.apply_wikilink`/`_wrap_occurrence`) instead of appending a `## See Also` bullet; candidates whose phrase doesn't literally occur in the body are filtered out before ever being offered (linking to an unmentioned topic is a deferred future feature, not implemented now) | User: applying a suggestion was adding "economics" to a See Also list instead of wrapping the existing word, and links to topics not mentioned in the text were being suggested — both flagged as unwanted right now | `f4954f7` |
| claude | Redesigned the `/wikilinks` walkthrough entirely: `vault.find_wikilinks()` + `StreamingText.show_links()` render existing `[[wikilinks]]` underlined and clickable, and new candidates in a distinct pending highlight; `Tab` cycles focus among pending candidates, empty `Enter` applies the focused one in place, `Ctrl+X` dismisses it — no chat-log messages at all, status lives in the statusbar (`"N pending wikilink(s) — ... — tab: next  enter: apply  ctrl+x: dismiss"`). Clicking a committed link navigates to that vault file (`ChatApp._open_wikilink_target`, offset-mapped via `StreamingText.on_click`/`_offset_at`) | User wanted pre-existing and newly-suggested wikilinks both highlighted persistently (not just the one-at-a-time chat flow), asked to explore the walkthrough's presentation, and chose "inline in preview only" plus doing the persistent-highlight/click-to-navigate work in the same session | `f4954f7` |
| claude | Fixed a scroll-position bug surfaced by click-to-navigate: `ChatApp._set_active_file` never reset `#preview-scroll`'s scroll position, so navigating to a shorter file while scrolled down on a longer one showed a blank preview | Found live-testing the new click-to-navigate feature — clicking a link appeared to do nothing until the scroll-reset fix landed | `f4954f7` |

## 2026-07-20

| Source | What | Why | Commit(s) |
|--------|------|-----|-----------|
| claude | Add `Vault.folder_tags()` — slugifies every ancestor folder name from a file up to (not including) vault root into a tag (e.g. `000 - Information Science` → `information-science`); `classify._suggest_tags` now merges these in ahead of the LLM's content tags on every `/tags` call, case-insensitively skipping any the file already carries | User was manually adding an `Information-Science` tag (plus others) to every file while working through that folder by hand and wanted `/tags` to add the folder-derived tag implicitly, without retyping the instruction per file | `_(this commit)_` |
| claude | Add a batch counterpart: selecting a folder node in the sidebar tree (not a file) and running `/tags` now calls new `classify.suggest_folder_tags`/`apply_folder_tags` to plan and apply the same folder-derived tags across every file under that folder in one pass, shown pre-checked in the existing suggestion checklist for review before `/done`; `FilePicker` folder nodes now carry their directory `Path` as `data` so the tree can distinguish a folder selection from a file selection | Doing this file-by-file for a whole folder was the exact repetitive step the user wanted to skip | `_(this commit)_` |
| claude | Fixed a pre-existing bug in `ChatApp._apply_checklist`: `_close_checklist()` wiped `_checklist_wikilink_kind` before the wikilinks branch read it, so checked inline/see-also wikilinks were silently never applied on `/done` | Found while adding the analogous `_folder_tag_by_label` lookup for the new batch flow — both dicts need to be snapshotted before closing the checklist, not after | `_(this commit)_` |
| claude | Updated a stale test (`test_suggest_classification_falls_back_to_llm_when_tags_unmapped`) whose fake-model prompt key referenced the old `_suggest_folder` prompt wording ("classifying a knowledge vault article") after an earlier, uncommitted session had already reworded the real prompt to "classifying an article into a folder" | Surfaced running the full unit suite for the first time since that wording changed; unrelated to this session's feature work but blocked a clean test run | `_(this commit)_` |
| claude | Restyled `SuggestionChecklist` to match `#log`'s background with no border (added `theme.CHECKLIST_MAX_HEIGHT = 20`, up from a hardcoded `10`); had to override both the base and `:focus` rules since `OptionList`'s own default CSS redraws a border on focus regardless of an app-level `border-top` override | A big batch checklist (e.g. the new folder-tags list) read as a cramped boxed dialog rather than more chat output | `_(this commit)_` |
| claude | Folder-tags checklist items now show `path.stem` ("Overview +a, info") instead of the full vault-relative path — every item is already known to be under the selected folder, so the path was redundant noise. Switched the underlying lookup from a `dict[label, (path, tags)]` to an ordered `list[(path, tags)]` matched by position (`SuggestionChecklist.checked_indices()`, new), since two files can share a filename across different subfolders and a label-keyed dict would silently collapse them into one checklist entry | User asked for filenames only, not full paths; verified the stem-only display would otherwise reintroduce a real collision bug and fixed it in the same pass | `_(this commit)_` |
| claude | Folder-tags rows now render as an unbordered two-column table — filenames left-padded to the batch's longest name so every tag list starts in the same column, tags colored `theme.TAG` (new: `#6d93c8`, a cool blue against the otherwise all-warm palette). `SuggestionChecklist` gained an optional `columns: list[(str, Text)]` param, decoupled from the plain `_items` strings that `checked_items()`/`checked_indices()` still resolve against | User asked for filenames-on-the-left/tags-aligned table styling with tags in a blue hue matching the color scheme | `_(this commit)_` |
| claude | Replaced the checklist's plain-`Text`-concatenation row rendering with a per-row `rich.table.Table.grid()` (checkbox/name/tags columns, fixed name-column width, no border) | The first version used `Text(name) + Text(tags)`; Rich `Text` has no concept of a hanging indent, so once a tag list wrapped to a second line the wrapped part fell back to column 0 instead of staying under the tags column — a `Table` cell wraps its own content and keeps continuation lines inside that cell's column, fixing it | `_(this commit)_` |
| claude | Added a blank-line gap between checklist rows (`SuggestionChecklist._build_options` now interleaves `None` — Textual's built-in option divider — after every row but the last; recolored via `.option-list--separator { color/background: bg }` so the default visible `───` rule renders as blank space instead) | User wanted vertical spacing between rows to better tell tag groups apart in a long folder-tags list; a divider was used over baking a blank line into each row's own `Table` (tried first) because a divider doesn't shift `checked_indices()` positions and isn't swept into the row's highlight block the way an extra table row would be | `_(this commit)_` |
| claude | Reset `background-tint: transparent` on `SuggestionChecklist`/`SuggestionChecklist:focus` | `OptionList`'s default CSS blends in a 5%-foreground tint while focused, which this widget almost always is once opened — every row (not just the actually-highlighted one) was rendering a permanent lightened background instead of the intended plain `#log`-matching one; same fix already applied to `#cmd:focus` for the same underlying Textual default | `_(this commit)_` |
| claude | Folder-tags batch now sanitizes as well as adds: `Vault.folder_tag_vocabulary()` (new) collects the set of tags any file's current folder ancestry could produce, vault-wide; `classify.suggest_folder_tags`/`apply_folder_tags` (now returning/taking `FolderTagChange(add, remove)`, new in `models.py`) strip any tag that matches that vocabulary but isn't one of the specific file's *current* ancestors — i.e. a stale tag left over from before the folder was moved — while leaving alone any tag that never corresponded to a real folder name (a genuine cross-referencing topic tag). Checklist rows show both, `+add` in `theme.TAG` blue and `-remove` in new `theme.TAG_REMOVE` (`#c2685f`, muted red) | User moved `OLD_STRUCTURE_MOVE_ME` from under `Study Notes/` to the project root and wanted `/tags` on that folder to strip the now-stale `study-notes` tag it had picked up at the old location, keeping tags "always sanitized and kept current with the existing folder structure" rather than just accreting more tags over time | `_(this commit)_` |
| claude | Folder-tags checklist's name column now truncates to 15 chars plus a `...` ellipsis (`theme.CHECKLIST_NAME_WIDTH`, new `app._truncate()` helper) rather than growing the column to fit whatever the longest filename in the batch happens to be | User wanted the folder side of the table capped so one long filename can't stretch the whole column and push the tags further right than necessary | `_(this commit)_` |
| claude | `theme.CHECKLIST_NAME_WIDTH` now derives from `round(CHAT_WIDTH * CHECKLIST_NAME_RATIO)` (0.4) instead of a flat `15` | User asked for the checklist's name/tags split to be 40%/60% of the chat column's actual width (`CHAT_WIDTH = 70`) rather than an arbitrary fixed character count | `_(this commit)_` |
| claude | Fixed `/ingest` never refreshing the sidebar tree — `_handle_ingest` now calls `FilePicker.reload(self.retriever.ingested_paths())` at the end, so a file added on disk outside the app (another editor, Obsidian) shows up as soon as `/ingest` finishes rather than needing an app restart. Every other `FilePicker.reload()` call site was reordered to run after its `reingest_one()` call (not before), so the coloring below reflects the manifest as it actually is post-action, not pre-action | User reported the file browser going stale even right after `/ingest` | `_(this commit)_` |
| claude | File tree now colors files against the ingest manifest: green (`theme.ADDITION`, reused) for a file on disk but not yet embedded, red (`theme.TAG_REMOVE`, reused) for a manifest path with no file on disk anymore — synthesized as a `[stale index]` ghost leaf under its last-known folder so it's visible and cleanable, not just silently gone. `Retriever.ingested_paths()` (new) reads the manifest for this; `FilePicker` gained `_status_by_path`/ghost-entry support in `_populate`, and `ChatApp.on_tree_node_selected` now guards against clicking a ghost (shows a status hint instead of crashing on the missing file) | User wanted the browser to always show the live vault state, with new/uningested and stale/orphaned-index entries visibly flagged rather than silently invisible | `_(this commit)_` |
| claude | Fixed a real, pre-existing bug surfaced while adding the `[stale index]` marker: `Tree.process_label` runs any plain `str` label through `Text.from_markup()`, which silently swallows `[...]` as an (invalid) style tag — the existing `" [misc]"` needs-placement marker had never actually been visible in the UI. Fixed by passing a `Text(label)` object to `add_leaf` instead of a bare string, which skips markup parsing entirely | Found because the new `[stale index]` suffix wasn't rendering either, during this session's own visual verification | `_(this commit)_` |
| claude | File tree now polls the vault every `app._FILE_TREE_POLL_SECONDS` (2s) via `FilePicker.refresh_from_disk()` (new), rather than only refreshing on explicit app actions (`/ingest`, a draft save, a classify apply) — a file dropped on disk by another editor now shows up (green) on its own, no `/ingest` or restart needed. The poll only actually rebuilds the tree when the file set changed or ingest status changed (cheap comparison first), and no-ops entirely while a search filter is active, so it can't reset expand/cursor state or yank away an in-progress search on every tick | User clarified after the `/ingest`-refresh fix that they wanted live disk changes to appear without running `/ingest` at all — `/ingest` should only be needed to actually embed a file, not to make it visible | `_(this commit)_` |
| claude | `FilePicker` tree now labels each leaf with `file.path.stem` (the actual filename) instead of `file.title` (the frontmatter `title:` field) | User renamed several files on disk without updating their `title:` field, and the tree kept showing the stale frontmatter title instead of the real filename — confirmed by diffing the two directly (`010 Information Retrieval` in frontmatter vs. the file actually named `001 Information Retrieval.md`). User: "make sure the file browser uses the file's name instead of frontmatter" | `_(this commit)_` |
| claude | `Vault.save_file` no longer writes a `title:` field to frontmatter at all (was always writing `File.title`, which itself falls back to the filename when frontmatter has none) — title is now purely derived from the filename on load, never persisted, so it can't drift from the real filename again | Direct follow-up to the file-browser fix: with the browser now showing filenames instead of titles, a separately-maintained `title:` field was just a source of future drift, not a feature being used for anything | `_(this commit)_` |
| claude | Cleaned up the `Formal Notes/000 - Information Science/000 Overview/` files (the 9 identified with stale titles, plus the 1 already in sync) by round-tripping each through `Vault.load_file`/`save_file` — `title:` is now gone from all 10, tags and body content unchanged | User: "can you clean up the frontmatter titles? just remove them entirely" — scoped to this folder specifically, not vault-wide | `_(this commit)_` |
| claude | Rebuilt the app layer around a tool registry + agent loop, replacing the four separate `ask.py`/`draft.py`/`classify.py`/`review.py` pipeline modules. New `utils/tools/` package: `ToolSpec`/`ToolRegistry`/`build_registry()` in `__init__.py`, `vault_tools.py` (`search_vault`, `read_vault_file`) and `web_tools.py` (`web_search`) as model-facing tools, `ask_tools.py`/`draft_tools.py`/`classify_tools.py`/`review_tools.py` porting each old module's logic as command-facing tools. New `utils/agent.py` (`run()`) wraps the tool-calling loop and bookkeeps sources/transcript. `app.py` now dispatches every command through `self.tools` instead of importing verb modules directly | The four-verb structure had become an arbitrary scaffold rather than a real architecture; the new model — every capability is a `ToolSpec`, whether a human invokes it via slash command or a model invokes it mid-loop — gives `/ask` a real toolbox to call and sets up future tools (write-capable ones, cross-command calls) without more restructuring. Full design discussion in `LLM-sandbox/conversations/2026-07-20_agent-tools-rebuild.md` | `_(this commit)_` |
| claude | `ModelClient` gained `stream_with_tools()` (`utils/llm.py`) — binds a `ToolSpec` list via `bind_tools`, loops model call → tool dispatch → feed result back, up to 4 rounds, then forces a final answer. `answer_question` (formerly `ask()`) now calls this instead of a single `model.stream()`, with `search_vault`/`read_vault_file` always available and `web_search` available whenever `/web` is on | Direct continuation of the toolbox plan for `/ask` — the model can now search again with different phrasing, open a note's full text, or check the web itself instead of being limited to one pre-fetched top-k | `_(this commit)_` |
| claude | Dropped the old manual web-supplement machinery (`_web_supplement`, `_build_web_prompt`, `source_covers_topic`, `AskResult.web_supplement`) — web access is now just the `web_search` tool, available on every retrieval path when `/web` is on, and the model decides for itself whether to use it instead of a heuristic "does the source cover the topic" gate | Simplification that fell out of giving `/ask` a real toolbox: the two-tier answer/web-supplement split was working around not having tool-calling, and is redundant once the model can call `web_search` inline | `_(this commit)_` |
| claude | `app.py`'s `_ask()` now passes an `on_tool_call` callback that writes `tool: search_vault(query='...')`-style status lines to the chat log as the agent loop runs, reusing the existing `_write_status` helper | User wanted visibility into what the model searched/read before answering, matching the "show tool calls inline" design decision | `_(this commit)_` |
| claude | Test suite reorganized to match: `test_ask.py`/`test_draft.py`/`test_classify.py`/`test_review.py` (+ their `_integration` siblings) renamed to `test_ask_tools.py` etc. under the new import paths; added `test_agent.py`, `test_tools_vault.py`, `test_tools_web.py`, and `stream_with_tools` coverage in `test_llm.py`. Also fixed a pre-existing bug surfaced during the port: `test_draft.py` imported a `start_draft` that had never existed in `draft.py` (only `edit_draft` did) — the test was already broken before this session, from an unfinished prior rename | All 138 tests (unit + integration, real Ollama reachable this session) pass post-rebuild | `_(this commit)_` |
| claude | Resolved the long-open "session-log location unresolved" note in `CLAUDE.md`: session chatlogs worth preserving in full now live in `LLM-sandbox/conversations/` (the folder name `.vaultignore` already reserved for this) | `.vaultignore` had carried a `conversations` entry with nowhere for it to point since the old `Knowledge/` vault was folded in; the design discussion behind this session's rebuild was the first thing judged worth saving this way | `_(this commit)_` |
| claude | Reconstructed and reformatted `Study Notes/Most Recent Conversation.md` (a debug-log copy/paste the user made) into a clean, fully-untruncated Q&A transcript, using screenshots to recover text the debug log had cut off at 400 chars | Debugging revealed `debug_log.truncate()` caps every logged response at 400 characters — the user's copy/paste was missing most of three of four answers; screenshots of the live TUI were the only complete record | `_(this commit)_` |
| claude | Added three backlog items to `config/ideas.md`: save `/ask` conversations in full + a way to distill them into vault articles (extends the existing "fold conversations into the KB" idea under Vault hygiene — this is also the "memorize" stage the rebuild deliberately left unimplemented); a bug where the chat log can't be scrolled while the agent is "thinking…"; a bug where the prompt input doesn't wrap long text and scrolls it off-screen instead | User hit all three live-testing the rebuilt app and asked for them tracked for next session | `_(this commit)_` |
| claude | `ModelClient.stream_with_tools()` rewritten to stream every round (`ChatOllama.astream()` + `AIMessageChunk` addition to accumulate `tool_call_chunks` into `.tool_calls`) instead of one non-streaming `ainvoke()` call — verified against real `llama3.1:8b` that tool-call rounds carry empty content and complete `tool_calls` only once fully accumulated, so this doesn't leak partial tool-call noise into `on_token`. `agent.run()`/`answer_question()` now thread a real `on_token` through; `app.py`'s `_ask()` mounts a temporary `Static` widget above the input bar that grows token-by-token, then removes it and commits the final text to `#log` | `/ask` previously showed "thinking…" then dumped the whole answer at once — no callers had ever wired `on_token` for the chat path. Verified end-to-end against real Ollama: 445 token callbacks whose concatenation exactly matched the final answer | `_(this commit)_` |
| claude | Recovered and applied an uncommitted `git stash` (`pending changes`) found sitting in the working tree from before this session: 78 already-superseded copies deleted from `Study Notes/OLD_STRUCTURE_MOVE_ME/` (each verified to have a matching current copy under the flattened top-level `OLD_STRUCTURE_MOVE_ME/` or `PDF Database/wiki_pages/` before removal, so nothing was lost — just finishing a move that was already half-done), `config/vault-structure-plan.md`'s revision marked "finalized" with Wikipedia-house-style dash/`&`→`and` cleanup, a small `Study Notes` note edit, and two real-but-undocumented `config/README.md` command-table entries (folder-scoped `/tags` sync, live file-tree polling) | The stash bundled real pending vault-reorg/doc work together with edits to `ask.py`/`draft.py`/`review.py`/`test_draft.py`/`config/ideas.md` that the rebuild had already fully superseded (confirmed line-by-line: the `start_draft`→`edit_draft` rename and the "personalized agentic mini Claude Code" vision note were both already captured) — those hunks were reviewed and left dropped rather than reapplied, since applying them would have resurrected dead code | `_(this commit)_` |
| claude | Left a second stash (`stash@{1}`, "Auto-stashing changes for checking out websites") untouched — inspection showed it predates the current `LLM-sandbox/ChatUI/` layout entirely (it moves `chatui.py`/`apply_update.py` *into* `ChatUI/`, which already happened, and deletes the old flat top-level `.md` vault files from before the `Formal Notes`/`Study Notes` split) | Auto-created by git on an old branch checkout, unrelated to any current work; reapplying it would regress the vault structure. Flagged for the user to `git stash drop` once confirmed safe, rather than dropped unilaterally | `_(this commit)_` |

## 2026-07-21

| Source | What | Why | Commit(s) |
|--------|------|-----|-----------|
| claude | Replaced `#log` (a `RichLog`) with `ChatLog` (`ui/chatlog.py`, new — a `VerticalScroll` of one `Static` widget per message) and a `StreamingMessage` handle whose `.update()` rewrites the same widget in place. `_ask()`'s streamed answer now mounts (on first token, not at call start, so it lands after any status lines already written) and grows in that one resting widget instead of a scratch `Static` above the input bar that got discarded and rewritten into the log once finished | User: the finished answer visibly jumped from just above the prompt box to elsewhere in the chat once streaming completed — traced to the old two-widget handoff (a `live` `Static` for in-progress tokens, then a fresh `RichLog` line for the final text) | `_(this commit)_` |
| claude | `ChatLog` only auto-scrolls to the bottom when the view was already there before a message/token arrived (checked via `scroll_y`/`max_scroll_y` before each mutation, applied via `call_after_refresh` after); scrolled-up history is left alone while new content lands off-screen | User wanted to be able to scroll up and read history while the agent is "thinking…"/streaming — matches the known bug already logged in `config/ideas.md` ("can't scroll `#log` while the agent is thinking") | `_(this commit)_` |
| claude | `ask_tools.answer_question()` gained an `on_status` callback, fired at retrieval start (`"searching vault…"`), after the retrieval path is chosen (`"found N relevant note(s), top score X"` / weak-match / no-match wording per path), and just before the model call (`"asking <model>…"`); threaded through `build_ask_tools()` and wired in `app.py`'s `_ask()` to the same `_write_status` status-line path already used for tool calls | User: the UI sat on "thinking…" with no visibility into what was happening for however long the local model took to produce a first token | `_(this commit)_` |
| claude | New `utils/transcript.py` (`TranscriptWriter`) appends every chat line (you/status/answer/hint) to a per-session Markdown file under `conversations/transcripts/`, opened and closed on each write so a crash loses at most the one in-flight line. `ChatApp` owns one instance (`self.transcript`, constructed in `__main__.py` alongside `Vault`/`Retriever`/`ModelClient`); the four `_write_*` helpers in `app.py` now write to both the `ChatLog` widget and the transcript from one call site. `conversations/transcripts/` added to `.gitignore` (already excluded from vault ingestion by the existing `conversations` rule in `.vaultignore`) | User wanted chat conversations preserved on program exit, including an unclean one — this was also the explicitly-deferred "memorize" stage from the 2026-07-20 agent/tools rebuild | `_(this commit)_` |
