# python program to work with obsidian
# creates a hierarchical link between notes, given a tree

subject = None  # highest level in the tree


def yaml_frontmatter(file, property, value):
    """adds yaml properties to the heading of a file"""

    # add tag
    # link parent file

def convert_tree_to_list(input):
    """returns a list of the subject hierarchy"""

    # assign depth level to every item
    # based on position in tree
    #
    # - depth 1
    #   - depth 2
    #   - depth 2
    # - depth 1
    #   - depth 2
    #     - depth 3
    #       - depth 4
    #     - depth 3
    #   - depth 2

def create_folder_structure()

def create_file(location, name):
    """"""

    #if !(file_exists)
        # only create a file if it does not exist already
        
        # update yaml_frontmatter at the top of the file
            # check to see if frontmatter is there already 
